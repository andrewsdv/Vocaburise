<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="android.view.View" />

        <variable
            name="vm"
            type="com.masterofcode.vocaburise.screens.MainActivityViewModel" />

    </data>

    <android.support.v4.widget.SwipeRefreshLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:onRefresh='@{() -> vm.fetchWords()}'>

        <RelativeLayout
            android:id="@+id/contentView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical">

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_above="@+id/wordsNumber"
                android:gravity="center_horizontal"
                android:padding="16dp"
                android:paddingBottom="8dp"
                android:paddingTop="16dp"
                android:text='@{vm.welcomeMessage}'
                android:textSize="18sp"
                tools:text="Welcome back, Andrew!" />

            <TextView
                android:id="@+id/wordsNumber"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_above="@+id/startButton"
                android:gravity="center_horizontal"
                android:paddingBottom="16dp"
                android:paddingTop="8dp"
                android:text='@{vm.wordsNumber}'
                android:textSize="16sp"
                tools:text="You have 10 words assigned for today" />

            <Button
                android:id="@+id/startButton"
                style="@style/Base.Widget.AppCompat.Button.Colored"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_centerInParent="true"
                android:layout_marginBottom="16dp"
                android:onClick='@{() -> vm.startLearning()}'
                android:text="@string/mainScreenStartLearning"
                android:textColor="@color/white"
                android:visibility="visible" />

            <FrameLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:visibility='@{vm.progressBarVisible ? View.VISIBLE : View.GONE}'
                tools:visibility="gone">

                <include layout="@layout/view_loading_overlay" />
            </FrameLayout>

            <android.support.design.widget.FloatingActionButton
                android:id="@+id/fab"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentBottom="true"
                android:layout_alignParentEnd="true"
                android:layout_margin="@dimen/fab_margin"
                app:srcCompat="@drawable/ic_add" />

        </RelativeLayout>
    </android.support.v4.widget.SwipeRefreshLayout>

</layout>